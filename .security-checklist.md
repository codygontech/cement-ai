# Security Best Practices - Quick Reference

## 🚫 Never Commit:
- `.env` files (use `.env.example` instead)
- API keys, passwords, tokens
- Service account JSON files
- Private keys (`.key`, `.pem`)
- Database credentials
- `__pycache__/` directories

## ✅ Always Use:
- Environment variables for secrets
- `.env.example` for documentation
- Google Secret Manager in production
- Strong random SECRET_KEY
- `.gitignore` to exclude sensitive files

## 🔐 Before Committing:
1. Check for hardcoded secrets
2. Verify `.env` is not staged
3. Review `.gitignore` is up to date
4. Run: `git status` to check staged files
5. Consider using pre-commit hooks

## 🆘 If You Commit Secrets:
1. IMMEDIATELY rotate all exposed credentials
2. Revoke keys in Google Cloud Console
3. Remove from git history (use BFG Repo-Cleaner)
4. Notify security team

## 📚 Full Guide:
See `SECURITY.md` for complete guidelines
